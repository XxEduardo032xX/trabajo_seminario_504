@model IEnumerable<app_ds501.Models.Metodo>

@{
    ViewData["Title"] = "Metodos de pago";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Listado de Metodos de Pago</h1>
<hr />

<div class="container">
    <div class="row">
        <div class="col-sm-3">
            <a class="btn btn-success" asp-controller="Metodo" asp-action="Create">Agregar Metodo de pago</a>
        </div>
    </div>

    <br />

    <div class="row">
        @if (Model.Count() > 0)
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <td>@Html.DisplayNameFor(met => met.codigo_metodo)</td>
                            <td>@Html.DisplayNameFor(met => met.nombre)</td>
                            <td>@Html.DisplayNameFor(met => met.descripcion)</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var reg in Model)
                        {
                            <tr>
                                <td>@reg.codigo_metodo</td>
                                <td>@reg.nombre</td>
                                <td>@reg.descripcion</td>
                                <td>
                                    <a asp-controller="Metodo" asp-action="Edit"
                                       asp-route-id="@reg.codigo_metodo" class="btn btn-outline-warning btn-sm">
                                        <i class="fas fa-edit"></i> Editar
                                    </a>

                                    <a asp-controller="Metodo" asp-action="Delete"
                                       asp-route-id="@reg.codigo_metodo" class="btn btn-outline-danger btn-sm">
                                        <i class="fas fa-trash-alt"></i> Eliminar
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <p>No existen registros</p>
        }
    </div>

</div>
